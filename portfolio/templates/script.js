const body = document.body;

const init = function () {
  loadInterface();
};

const loadInterface = function () {
  const load_div = document.createElement("div");
  load_div.id = "loading";
};

init();
